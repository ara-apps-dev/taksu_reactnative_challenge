import React from 'react';
import { View, Text } from 'react-native';

const LoginScreen = () => {
  return (
    <View>
      <Text style={{ color: 'black', fontSize: 20 }}>LoginScreen</Text>
    </View>
  );
};

export default LoginScreen;

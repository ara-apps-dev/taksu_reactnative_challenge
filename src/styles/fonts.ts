import { fontStyle } from '@utils/fontStyle';
import { Colors } from './colors';

export const textH1 = fontStyle({
  variant: 'regular',
  weight: 'bold',
  size: 30,
  color: Colors.textWhite,
});

export const textH2 = fontStyle({
  variant: 'regular',
  weight: 'bold',
  size: 20,
  color: Colors.textWhite,
});

export const textH3 = fontStyle({
  variant: 'regular',
  weight: 'bold',
  size: 15,
  color: Colors.textWhite,
});

export const label1 = fontStyle({
  variant: 'regular',
  weight: 'regular',
  size: 16,
  color: Colors.textWhite,
});

export const label2 = fontStyle({
  variant: 'regular',
  weight: 'regular',
  size: 15,
  color: Colors.textWhite,
});

export const label3 = fontStyle({
  variant: 'regular',
  weight: 'bold',
  size: 10,
  color: Colors.textWhite,
});

export const buttonLabel1 = fontStyle({
  variant: 'regular',
  weight: 'bold',
  size: 15,
  color: Colors.textWhite,
});
